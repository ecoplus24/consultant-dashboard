# 매매 백테스트 엔진 의존성
# 루트 requirements.txt(스크레이퍼용)와 분리해 둔다.
# 스크레이퍼 GitHub Action이 불필요하게 무거워지지 않게 하기 위함이다.
#
#   pip install -r trading/requirements.txt

pandas>=2.0.0
numpy>=1.24.0

# 데이터 공급자 (없어도 엔진과 CSV/합성 데이터는 동작한다)
yfinance>=0.2.40   # 해외 주식
pykrx>=1.0.45      # 국내 주식 — KRX_ID / KRX_PW 환경변수가 필요할 수 있음

# 테스트
pytest>=7.4.0
